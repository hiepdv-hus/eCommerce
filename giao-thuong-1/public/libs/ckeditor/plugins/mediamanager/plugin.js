// Image managerCKEDITOR.plugins.add( 'mediamanager', {    icons: '',    init: function( editor ) {    	//editor = editor || window['editor'];        // Plugin logic goes here...        editor.ui.addButton('addimage',{        	label: 'Chèn ảnh',        	title: 'Chèn ảnh',        	icon: 'image',        	command: 'addimage',        });        editor.addCommand('addimage', {        	exec: function(editor){        		F.Media.manage(editor);        	}        });    }});